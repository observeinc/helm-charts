{{ if .Values.observeGateway.enabled  -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway
  namespace: {{ template "observe-agent.namespace" . }}
data:
  relay: |
      {{- include "observe.deployment.applyGatewayConfig" . | nindent 4 -}}
{{ end -}}
